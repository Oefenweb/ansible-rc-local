# tasks file for rc-local
---
- name: update init script
  template:
    src: etc/rc.local.j2
    dest: /etc/rc.local
    owner: root
    group: root
    mode: 0755
    validate: '/usr/bin/env sh -n %s'
  tags:
    - configuration
    - rc-local
    - rc-local-init
