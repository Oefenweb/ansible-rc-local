# test file for rc-local
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: include vars
      include_vars: main.yml
  roles:
    - ../../
  post_tasks:
    - include: post.yml
